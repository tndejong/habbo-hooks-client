# OpenClaw Hooks (Planned)

This domain folder is reserved for future OpenClaw hook installers and event mappings.

Planned structure:

- `hooks/openclaw/install.sh`
- `hooks/openclaw/status.sh`
- `hooks/openclaw/uninstall.sh`

Shared runtime components will continue to live in `hooks/` root.
